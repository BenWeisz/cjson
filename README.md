# cjson

just a header file for reading json files, that's all you get.
it's actually pretty inefficient, but gets the job done. 

later if i'm bored i might add a custom block allocator for the data and a linear probe hashtable capabilities to speed up lookups.
